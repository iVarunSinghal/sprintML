% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_submission.R
\name{make_submission}
\alias{make_submission}
\title{Generate a Competition-Ready Submission File}
\usage{
make_submission(
  model,
  newdata,
  id_values,
  id_col = "id",
  target_name = "target",
  file = "submission.csv"
)
}
\arguments{
\item{model}{A fitted model (e.g. from \code{quick_baseline()}).}

\item{newdata}{Data frame of test-set predictors.}

\item{id_values}{Vector of ID values, one per row of \code{newdata}.}

\item{id_col}{Name for the ID column in the output. Default "id".}

\item{target_name}{Name for the prediction column. Default "target".}

\item{file}{Path to write the CSV to. Default "submission.csv".}
}
\value{
Invisibly returns the submission data frame.
}
\description{
Generate a Competition-Ready Submission File
}
\examples{
\donttest{
df <- data.frame(x = rnorm(30), y = factor(sample(c("a", "b"), 30, replace = TRUE)))
train_data <- caret::train(y ~ ., data = df, method = "glm",
                           trControl = caret::trainControl(method = "none"))
}
}
